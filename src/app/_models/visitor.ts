export interface Visitor {
    _id: string;
    name: string;
    contact: string;
    email: string;
    whomToMeet: string;
    signatureImagePath: string;
    profileImagePath: string;
    isLaptop: Boolean;
    serialNumber: string;
    signOut: string;
    company: string;
}